const config = {
  reactStrictMode: true,
  // Add your other configuration options here

  // Add this line to skip environment variable validation during the build process
  skipEnvValidation: true,
};

export default config;
